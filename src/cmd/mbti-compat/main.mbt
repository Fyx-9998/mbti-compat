///| Placeholder entry point. Replaced with the real CLI (see the design brief's
/// M1/M2 roadmap: `check`, `report`, `--baseline`, `--declared`, exit codes
/// 0/1/2/3) once ingest, compare, policy, and report exist.
fn main {
  println("mbti-compat: scaffold only, not yet implemented")
}
